# T-07 (Anbau) Deep Dive — Pre-Walk Readiness Report

**Date:** 2026-06-12 · **Repo state:** main @ `79c999c` (clean tree) · **Mode:** READ-ONLY — no code, directive, or corpus changes were made.

**Bayern SHA verified at audit time:** `41b37961e1cc8f5e166247ef8f8fed816ddaac4861ecf1638a337b9e80472fb8` — `node scripts/verify-bayern-sha.mjs` → ✓ MATCH (composed-prefix 95,665 chars). Unchanged, as required.

**Method:** the T-01..T-06 walk methodology (2 live walks per template — deep + thin state; audit all surfaces; class-level fixes; live confirm). This report is the pre-walk audit. Evidence is `file:line` against `79c999c` throughout.

---

## §0 Known-open ledger verification (verify-then-exclude)

The brief lists items believed open and recorded. Verdict per item — items recorded are EXCLUDED from findings below; items **missing from the repo ledger are themselves findings** (the knowledge exists only in session memory, which the repo cannot replay).

| Known-open item | Recorded? | Where |
|---|---|---|
| Emission-order rule (facts before prose) | ✅ | `docs/SPRINT_PLAN.md:398-401` (directive-bundle item 1) |
| §3 ASSUMED-positive example (grounded-emission valve) | ✅ | `docs/SPRINT_PLAN.md:402-405` (item 2) |
| C-1 double pin — GK temporal meaning + FOK height measure (eaves-proxy fix) | ✅ directive-level | `docs/SPRINT_PLAN.md:406-418` (item 3); BW-cell instantiation in `docs/LEGAL_REVIEW_LEDGER.md` LRL-1 |
| Contract promotion: `gk_sprung` / `stellplatz_*` / `kenntnisgabeverfahren_anwendbar` | ✅ | `docs/SPRINT_PLAN.md:418-426` (item 4) |
| Lex-specialis-unapplied → ASSUMED valve | ✅ | `docs/SPRINT_PLAN.md:427-433` (item 5) |
| TYPISIERTE-FAKTEN template scoping | ✅ | `docs/SPRINT_PLAN.md:435-444` (item 6) |
| München cost-caption (cost bands cite München/BayBO for non-Bayern) | ✅ partial | `BACKLOG.md:72-75` (`COST_BANDS_BY_TEMPLATE` Bayern-bleed — only T-04's basis text cleaned) |
| **A.1/B.1 Bayern meta-leak in turn 1** (PLZ table + ZITATE-DISZIPLIN ship in all 16 states' prompts; relocation owed) | ❌ **NOT RECORDED** | nowhere in SPRINT_PLAN, LEGAL_REVIEW_LEDGER, or BACKLOG |
| **Eaves-proxy GK in non-BW states** (C-1 pin instantiated only in the BW cell; 15 other cells lack it) | ⚠️ PARTIAL | the directive-level pin is item 3; the per-cell gap for the other 15 states is recorded nowhere |
| **Cost-basis key naming** (COST_BASIS_FIELD_BY_TEMPLATE keys never directive-named) | ❌ **NOT RECORDED** | absent from SPRINT_PLAN bundle and `docs/CAPTURE_CONTRACT_COVERAGE.md` (which covers `planungsrecht_paragraph`/`bruttoraumflaeche_m3` but not the area keys) |
| **D-1 Art. 76 gloss** | ❌ **NOT RECORDED** | no repo doc carries it |
| **Bayern base-block heading-gate hole** | ❌ **NOT RECORDED** | the runtime gate's state-keyed skip exists in code (`citationHeadingGate.ts:140` — "no canonical for this state → skip") but no ledger entry names the Bayern base-block exposure |
| **§§ 15/38 LBO BW heading-check** | ❌ **NOT RECORDED** | (corpus check this audit: § 15 "Brandschutz" / § 38 "Sonderbauten" headings do match — see §5; the *task* of recording the check is what's missing) |
| **§ 51 NBauO corpus verify** | ❌ **NOT RECORDED** as open | NI corpus *has* § 51 = "Sonderbauten", heading-only, `verification_tier: secondary-mirror` (`scripts/legal-corpus/states/niedersachsen.json`, key `'51'`) — primary-source confirmation never logged |

> **Finding 0 (recording debt):** five known-open items + one partial exist only in session memory. If the operator's memory is lost, the repo cannot reconstruct them. They should be filed in SPRINT_PLAN's deferred sections at the next docs touch (not this read-only turn).

Everything in the table above is **excluded** from the findings below; nothing in §§1–7 re-discovers them.

---

## §1 Composition chain — end to end

Full 40-hop trace performed; condensed here to the decision points. Verdict tags: **[T-07]** specific path · **[PARAM]** template-parameterized · **[DEFAULT]** falls through to a default branch.

| # | Hop | Evidence | Verdict |
|---|---|---|---|
| 1 | Intent: explicit wizard pick of the `anbau` sketch card — **no keyword detection exists** | `src/features/wizard/components/QuestionIntent.tsx:39`; `src/features/wizard/lib/selectTemplate.ts:33-42` | [T-07] |
| 2 | `selectTemplate`: `case 'anbau': return 'T-07'` (exhaustive 8-way switch, no default arm) | `selectTemplate.ts:108-109` | [T-07] |
| 3 | Persisted twice: `intent='anbau'` + `template_id='T-07'`; `initialProjectState` seeds `state.templateId` | `useCreateProject.ts:127,223,266`; `projectStateHelpers.ts:269-273` | [T-07] |
| 4 | Registry: `BLOCKS['T-07'] = T07_ANBAU_BLOCK`; `getTemplateBlock('T-07', bundesland)` = base + `'\n\n'` + non-null override cell | `templates/index.ts:36-45,61-73` | [PARAM] |
| 5 | 15 authored state cells under `'T-07'`; Bayern deliberately absent (base block IS Bayern) | `stateOverrides.ts:4952` (entry), cells §2 below; design note `:59-66` | [T-07] |
| 6 | Edge prompt: Block 2 = `getTemplateBlock(templateId, bundesland)`; live state carries `templateId: T-07` | `chat-turn/systemPrompt.ts:248-279,:59`; `chat-turn/index.ts:285-286,386,409,660` | [PARAM] |
| 7 | First-turn primer has a dedicated `'T-07'` arm (opens with 75 m³ BRI + Innen-/Außenbereich) | `chat-turn/index.ts:829-830` | [T-07] |
| 8 | `intentFromTemplate('T-07') → 'anbau'` explicit case | `resolveProcedure.ts:87-88` | [PARAM] |
| 9 | Procedure tiers: anbau shares the `neubau‖aufstockung‖anbau` branch; zero-verdict default = `vereinfachtes` + state simplified § + CALCULATED, anbau-worded rationale. **The 75 m³ tier is never computed mechanically — prompt-prose only** | `resolveProcedure.ts:924-958` (branch), `:659/:684/:711/:741/:770` (verdict keywords), `:973-1051` (citation classifier) | [PARAM] / [DEFAULT] for the threshold |
| 10 | Cost basis: `COST_BASIS_FIELD_BY_TEMPLATE['T-07'] = ['anbau_flaeche_m2']`; miss → corpus regex → 180 m² `BASE_AREA_SQM` | `costNormsMuenchen.ts:341,:299-305,:355` | [T-07] entry, [DEFAULT] chain behind it |
| 11 | Cost display: T-07 ∈ `HEADLINE_BAND_TEMPLATES`; sourced band €4,500–18,000 — **the ancestral exportPdf fallthrough is CLOSED**: PDF cost section's `isDemolition→isUseConversion→isRenovation→engine→band` chain routes T-07 to the band arm, not the München EFH table | `costNormsMuenchen.ts:596-601,624-629,680-689`; `exportPdf.ts:734-818` (band arm `:799-818`, v1.0.33 C2 comment) | [T-07] |
| 12 | Roles: `case 'anbau': return NEW_BUILD_ROLES(citations,'bestand')` — Bestand Vermesser rationale | `deriveBaselineRoles.ts:385-386,92-133` | [PARAM] |
| 13 | **Documents: NO anbau branch** — generic permit set; structural/Brandschutz docs only if `eingriff_tragende_teile` captured; misses the neubau baseline block (`:251-320`, gated `intent==='neubau'`) | `requiredDocuments.ts:126-399` | **[DEFAULT → generic permit set]** |
| 14 | Do-next: anbau folded into `isNewBuild` → T-01 baseline steps; no 75 m³ / Abstandsflächen step | `composeDoNext.ts:128,139-175` | **[DEFAULT → T-01 steps]** |
| 15 | GK: `TEMPLATE_FREISTEHEND_DEFAULT['T-07'] = false` (explicit, conservative); **but `gkDerivationCarveOut('T-07') → null`** — only T-04/T-06 carve out | `deriveGebaeudeklasse.ts:99` (table `:82-100`), `:123-129` | [T-07] / **[DEFAULT → fresh derived-GK row]** |
| 16 | Timeline: `if (intent !== 'abbruch') return 'bauantrag'` — full Bauantrag Gantt incl. "Baugenehmigung issued" milestone even on a verfahrensfrei/anzeige outcome | `composeTimeline.ts:196-199`; `exportPdf.ts:823-872,826-827` | **[DEFAULT → DEFAULT_TIMELINE_PHASES]** |
| 17 | Exec read: anbau folded into the new-build conditions wording (no 75 m³ condition) | `composeExecutiveRead.ts:339-348` | **[DEFAULT → new-build clause]** |
| 18 | PDF label: `'template.T-07': 'T-07 · Extension'` exists (absent-templateId default is T-01) | `exportPdf.ts:273-274`; `pdfStrings.ts:53` | [T-07] |
| 19 | PDF Section 05 / Key Data / Exec Top-3 / Section VIII / .md: all decision-first or shared-selector (`resolveRecommendations`, `isDeliverableFactKey('T-07')`) | `exportPdf.ts:382-386,902-943,1153-1230,416,1078`; `exportMarkdown.ts:84,125,249` | [PARAM] |
| 20 | Capture/persist: `extracted_facts` free-form, template-blind upsert; atomic RPC, no template logic | `toolSchema.ts:140-154`; `projectStateHelpers.ts:383-410,798-812`; `persistence.ts:372-420` | [PARAM] |
| 21 | Traces: no `template_id` column on the trace row; queryable only as a `chat_turn.load` span attribute | `tracer.ts:84-100`; `chat-turn/index.ts:267-271` | [DEFAULT → template-less trace] |

**Default-fallthrough inventory for T-07** (the exportPdf-isDemolition ancestral class — proven closed for cost, hop 11; still live at):
1. **Documents** — `requiredDocuments.ts:126-399` (thinnest surface: `t07-anbau.ts:55-57` calls Tragwerk-Anbindung typical, yet no Standsicherheit doc ships without a captured eingriff fact).
2. **Do-next baseline** — `composeDoNext.ts:128`.
3. **GK carve-out exclusion** — `deriveGebaeudeklasse.ts:123-129` (see §7 class 12).
4. **Timeline Gantt** — `composeTimeline.ts:196-199`.
5. **Exec-read conditions clause** — `composeExecutiveRead.ts:339-348`.
6. **Procedure threshold** — 75 m³ exists only in prompt prose (`t07-anbau.ts:30,38-41,235-250`); resolver has no tier math (`resolveProcedure.ts:924-958`).
7. **Cost-area fallback** — 180 m² backstop `costNormsMuenchen.ts:355` (headline shielded by band mode).
8. **Quarantine asymmetry** — T-07 owns zero scoped keys in `systemFlagFilter.ts:52-56`; only `abbruch_typ` is quarantined FOR it, nothing is quarantined OF it.
9. **Trace row** — `tracer.ts:84-100`.
10. **Latent**: `buildProcedureCase` reads `state.templateId ?? 'T-03'` (`resolveProcedure.ts:1497`) — an unhydrated state would degrade T-07 to Sanierung intent (guarded in practice by wizard seeding + `hydrateProjectState`).

---

## §2 Built / stubbed / deferred — the 16 rows

Base block: `src/legal/templates/t07-anbau.ts` (255 lines; `T07_ANBAU_BLOCK` at `:18-255`; "Reviewed by Bayern-zertifizierter Architekt: ☐ pending" `:15`). Fully REAL and **quantified**: 75 m³ BRI verfahrensfrei anchor (Art. 57 Abs. 1 Nr. 1 a), Art. 57 Abs. 7 Anzeige, Art. 59/60, Art. 6 (Abs. 5a 1,0 H München / 16-m-Privileg), BauGB §§ 34/35, GEG § 10, worked volume examples (`:247-250` "4 × 4 × 3 m ergeben 48 m³"), VERBOTENE-ZITATE block. Bayern-isms: München cost frame `:117-136`, StPlS 926 `:170`, ADBV `:88`.

> **Base-block anomaly (new):** `t07-anbau.ts:45` reads "Art. 35 Abs. 1" for the BauGB provision — § is the correct marker; the file's own VERBOTENE ZITATE section polices exactly this Art./§ confusion. Lint-harmless today, but it is instruction text the persona imitates.

Registry is **additive** (`stateOverrides.ts:1-38`): every state also sees the Bayern-shaped base; each cell opens with "ersetzen sämtliche oben genannten Bayern-Verweise". `'T-07'` entry: `stateOverrides.ts:4951-5619`, 15 cells, Bayern intentionally absent (`:66`). Thin-banner predicate: `STATES_WITH_FULL_STATE_BLOCK = {bayern, nrw, bw, hessen, niedersachsen}` in `src/legal/demoCoverage.ts` — banner ON for the other 11 regardless of cell quality.

| State | Cell lines (count) | Operative §§ (LBO unless noted) | Anbau-specific? | Banner | Last-authored |
|---|---|---|---|---|---|
| Bayern | base block only | Art. 57(1)1a, 57(7), 59, 60, 6/6(5a); BauGB 34/35; GEG 10 | **REAL, deepest (quantified)** | no | base file |
| BW | 4953–4995 (43) | §§ 50, 51 Kenntnisgabe, 52, 5, 6, 7, 13, 15, 27c Brandwände, 37, 53, 63, 43 + BauGB/BauNVO/GEG | **REAL** — only Flächenland cell w/ verfahrensfrei § + Kenntnisgabe | no | `8914775` 2026-05-28 (B2 batch-3) + `1763dfd` suffix fix |
| Hessen | 4996–5035 (40) | §§ 64, **64a erw. Freistellung**, 65, 6, 12, 68, 14, 52, 69, 67 | REAL — § 64a genuine HE nuance; **no verfahrensfrei §, no Brandwand §** | no | `307ace9` 2026-05-28 (B2 batch-4) |
| NRW | 5036–5074 (39) | §§ 63, 64, 6, 12, 68, 14, 30, 48, 70, 67 | REAL — **no verfahrensfrei §**; only cell claiming primary-source verification (`:5043`) | no | `39bd6ea` 2026-05-28 (B2 batch-1) |
| Niedersachsen | 5075–5117 (43) | §§ 60, 62, 63, 5, 7, 12, 65, 14, 30, 46, 47, 48, 67, 53 | **REAL — richest §-set (14), incl. verfahrensfrei tier** | no | `81fe204` 2026-05-28 (B2 batch-2) |
| Berlin | 5118–5159 (42) | BauO Bln §§ 62, 63, 6, 12, 66, 14, 26, 49, 68, 65 | REAL (categorical) | **yes** | `7c8a5e6` 2026-05-28 (C1) |
| Hamburg | 5160–5201 (42) | HBauO §§ 62, 63, 6, 12, 66, 14, 30, 49 "Grundstücksbezogene Mobilität", 68, 65, 54 | REAL (categorical) | **yes** | `f8874f8` 2026-05-28 (C2) |
| Bremen | 5202–5244 (43) | BremLBO §§ 62, 63, 6, 12, 66, 14, 30, 49, 68, 65, 54 | REAL (categorical) | **yes** | `c25871d` 2026-05-28 (C3) |
| Sachsen | 5245–5290 (46) | SächsBO §§ 62, 63, 6, 12, 66, 14, 30, 49, 68, 65, 54 | REAL (categorical) | **yes** | `466fc59` 2026-05-28 (C4) |
| SH | 5291–5338 (48) | LBO SH §§ 62, 63, 6, 12, 66, 14, 30, 49, 68, 65, 54 | REAL (categorical) | **yes** | `e1a4d2b` 2026-05-28 (C5) |
| RLP | 5339–5382 (44) | LBauO §§ 66, 67 (inverted numbering), 8, 13, 15, 30, 47, 63, 64, 56 | REAL (categorical) | **yes** | `79ee583` 2026-05-28 (C6) |
| MV | 5383–5429 (47) | LBauO M-V §§ 62, 63, 6, 12, 66, 14, 30, 49, 68, 65, 54 | REAL (categorical) | **yes** | `6a6915d` 2026-05-28 (C7) |
| Sachsen-Anhalt | 5430–5476 (47) | BauO LSA §§ 61, 62, 6, 12, 65, 14, **29 Brandwände** (only ≠§30 state), 48, 67, 64, 53 | REAL (categorical) | **yes** | `f783179` 2026-05-28 (C8) |
| Thüringen | 5477–5522 (46) | ThürBO §§ 64, 65, 6, 12, 72, 14, 33 Brandwände, 52, 74, 67, 57 | REAL (categorical) | **yes** | `dd358a5` 2026-05-28 (C9) |
| Brandenburg | 5523–5570 (48) | BbgBO **§ 62 Bauanzeigeverfahren (BB-unique institute)**, 63, 6, 12, 66, 14, 30, 49, 68, 65, 54 | REAL (categorical) | **yes** | `d13e6c3` 2026-05-28 (C10) + `a5f7ebb` §-renumber fix |
| Saarland | 5571–5618 (48) | LBO Saarland §§ 63, 64, 7, 13, 67, 15, 30, 47, 69, 66, 54 | REAL (categorical) | **yes** | `3b81c77` 2026-05-28 (C11) |

Structural facts: **no GENERIC-skeleton T-07 cell exists** (the "Mindest-Eckdaten" framing lives in the 11 thin systemBlocks, not the cells); all 15 cells share the 7-section skeleton (ROUTING/VERFAHREN/GEBIETSVERTRÄGLICHKEIT/TECHNIK incl. Brandwand line/STELLPLÄTZE/ENERGIE/ANTRAG); **REAL but categorical — no state cell quotes a numeric threshold** (no 75-m³ analogue, no H-factor, no metre distances); all 15 authored in the single 2026-05-28 B2/C-batch sweep; thin cells additionally carry the honest HINWEIS + "Stand:" date (e.g. Berlin `:5127-5128`, RLP `:5349`).

---

## §3 Pre-noted Finding #1 — anzeige wording mislabels an Anbau as Beseitigung (VERIFIED, report-only)

**The strings.**
- `src/legal/resolveProcedure.ts:421-424` — `procedureLabel` kind table: `anzeige: { de: 'Anzeigeverfahren (Abbruchanzeige)', en: 'Notification procedure (demolition notice)' }`. The table is `Record<ProcedureKind,{de,en}>` (`:410`) — **no template/intent parameter exists in the signature**.
- `resolveProcedure.ts:711-729` — the intent-BLIND anzeige verdict branch: keyword test `:711` (`/anzeigepflichtig|anzeigeverfahren|abbruchanzeige|beseitigungsanzeige|notification (?:required|procedure|requirement)/`), reasoning `:717-718` "**Anzeigepflichtige Beseitigung** … / Notification-required **demolition** …", and `:713` cite-less fallback `beseitigungCitationFor(c.bundesland)` — **the state's demolition §** stamped onto an Anbau.

**How T-07 gets there.** The branch runs before every intent branch (the fact-tree anzeige returns at `:1115-1177` are safely inside `if (c.intent==='abbruch')` `:1069`; the keyword branch is not). And T-07's own directive instructs exactly this vocabulary: `t07-anbau.ts:68` ("Anzeige nach Art. 57 Abs. 7 ist auch hier anwendbar"), `:186` (`✓ "BayBO Art. 57 Abs. 7" — Anzeigepflicht …`), and the Brandenburg cell `stateOverrides.ts:~5542-5547` ("Bauanzeigeverfahren nach § 62 BbgBO" — substring `anzeigeverfahren` matches `:711`). A Bayern T-07 verdict "anzeigepflichtig nach BayBO Art. 57 Abs. 7" → kind `anzeige`, label "(Abbruchanzeige)", reasoning "Anzeigepflichtige Beseitigung nach BayBO Art. 57" (`extractProcedureCitation` `:188` drops "Abs. 7"). A cite-less verdict is worse: the citation falls back to the demolition §. Bayern is not exempt (`resolveProcedures.ts:100-110` runs resolveProcedure for all states; the Bayern carve-out is cost-only).

**Surface trace.**

| Surface | Evidence | T-07 anzeige renders |
|---|---|---|
| Wizard | no `procedureLabel` consumer in `src/features/wizard/` | n/a |
| Web procedure card / Exec primary | `resolveProcedures.ts:30-35` | **LEAKS** — "Anzeigeverfahren (Abbruchanzeige)" + Beseitigung rationale |
| At-a-Glance | `AtAGlance.tsx:46-57` | **LEAKS** (same title) |
| Legal-landscape anchor row | `composeLegalDomains.ts:36-53` | clean — neutral "anzeigepflichtig" |
| PDF Section 05 | `exportPdf.ts:932` + decision body | **LEAKS** |
| PDF Key Data "Verfahren" row | `exportPdf.ts:1176-1177` | **LEAKS** |
| .md export | `exportMarkdown.ts:124-130` | **LEAKS** |
| Timeline/Gantt | `composeTimeline.ts:196-199` (intent-gated) | demolition lanes don't leak; secondary: full Bauantrag Gantt on a no-permit kind |
| Roles | `resolveRoles.ts:152-158` (intent-gated) | clean |
| Documents | `requiredDocuments.ts:136-141` (intent-gated) | clean; secondary: generic permit docs on a notification-only decision |
| Edge directive | `t07-anbau.ts:68,101,186`; chat-turn has no Beseitigung-anzeige prose | clean |

**CLASS verdict: (a) + (c), not (b).** (a) A shared wording table keyed by kind only — one entry per `ProcedureKind`, demolition wording baked into the single anzeige entry. (c) The T-05 sprint authored the verdict-honoring anzeige branch assuming anzeige is only reachable from abbruch; the keyword trigger made it reachable from any template, and exactly the kind-keyed surfaces leak while every intent-gated consumer stays clean. Not copy-paste — there is no second table. Note: a correct neutral wording already exists unused for this purpose at `stateLocalization.ts:91-95` (Bayern `notification` pack: "Anzeigeverfahren", Art. 57 Abs. 7).

**Guard status: UNGUARDED.** The literal "Anzeigeverfahren (Abbruchanzeige)" string is pinned nowhere in `scripts/` (zero hits); `smoke-t07-composer.mts` contains zero anzeige/Beseitigung assertions; `smoke-t05-composer.mts` pins the anzeige machinery only in abbruch context (kind `:144,:220`, doc `:148`, role `:152`, timeline strings `:154-158,:277-295` — F10 pins web==PDF byte equality a future fix must keep green).

---

## §4 Capture-key contract (CLASS-2 lens)

`extracted_facts` is **free-form** — `toolSchema.ts:140-154`: `key: { type: 'string' }`, no enum/pattern. Edge-side key awareness is only `factPlausibility.ts:46-63` (bounds for 6 numeric keys + 2 GK variants + `baugb_paragraph`). Drift notes: the directive's canonical `gebaeudeklasse` has **no** plausibility set (only `_geplant`/`_hypothese` variants do); the directive names `planungsrecht_paragraph` while plausibility validates the never-directive-named `baugb_paragraph`.

**T-07 sources name ZERO typed keys.** All directive naming lives in the universal `personaBehaviour.ts` blocks (A.4 `:162-182`, A.5/D.5 `:184-301`) shipped to every template; `t07-anbau.ts` (255 lines) and all 15 T-07 cells are pure prose — no `key:` mandate anywhere.

Quarantine registry (`systemFlagFilter.ts:52-56`) is exactly one entry — `abbruch_typ: ['T-05']`. **T-07 is named nowhere**; it is protected against `abbruch_typ` on all four deliverable surfaces (`exportPdf.ts:1157`, `exportMarkdown.ts:249`, `computeOpenItems.ts:50`, `qualifierAggregate.ts:51`) and owns no scoped keys of its own.

Key-by-key (DN = directive-named in personaBehaviour.ts; full table in agent evidence, condensed to the Anbau-relevant set):

| Key/concept | DN | Readers | Verdict |
|---|---|---|---|
| `gebaeudeklasse` ("GK\<N\>") | `:196-199` | `resolveProcedure.ts:1477-1486`; `costNormsMuenchen.ts:490-503`; `AtAGlance.tsx:193-199`; `exportPdf.ts:1198` | SAFE; T-07 not in carve-out (see §7-12) |
| `verfahren_indikation` (6-form pinned vocab) | `:242-250` | `resolveProcedure.ts:214-219,236,608-746` | SAFE channel, **but no Kenntnisgabe form — see risk 1** |
| `eingriff_tragende_teile` | `:222,269-275` | `resolveProcedure.ts:1499`; `resolveRoles.ts:205` (forces structural); `requiredDocuments` docCase | SAFE — de-facto statik/Bestand channel; **no `bestand_statik` key exists** |
| `grenzstaendig` | `:230` | `resolveProcedure.ts:1504`; `requiredDocuments.ts:355-370` | SAFE — only logic-bearing Grenzbebauung key (exact-match) |
| `planungsrecht_paragraph` | `:264-266` | `exportPdf.ts:505-517` | SAFE — the Innen-/Außenbereich channel |
| `gebaeude_freistehend`, `denkmalschutz`, `ensembleschutz`, `mk_gebietsart`, `bauvoranfrage_hard_blocker`, Sonderbau keys | named | procedure-case + confidence + risks readers | SAFE |
| **`anbau_flaeche_m2`** | **NO — named nowhere** | **`costNormsMuenchen.ts:341` — the sole T-07 cost basis** (header `:316-328` admits it's a "user-supplied guess") | **INVERSE CLASS-2, HIGH** — see risk 2 |
| **Volume / Brutto-Rauminhalt (75 m³)** | **no key exists** | **NONE** (`bruttoraumflaeche_m3` is a T-05-scoped reader guess, `:339`) | **MISSING CHANNEL, HIGH** — see risk 3 |
| Abstandsflächen status | NO | display-only (`humanizeFact.ts:110-113`) | CLASS-2 — see risk 4 |
| `grenzbebauung` variant | NO | none (only `grenzstaendig` reads) | MEDIUM variant-key risk |
| GRZ/GFZ reserve | NO | display-only | CLASS-2 low |
| Brandwand/Brandschutz boundary | NO | none (labels only) | CLASS-2 — every cell has the § 14/§ 30-class line |
| GEG scope / beheizt | NO | `geg_trigger` is **computed** = `eingriff_aussenhuelle && fassadenflaeche_m2>0` (`exportPdf.ts:968-970`) | CLASS-2 — see risk 5 |
| `gk_sprung`, `stellplatz_*`, `kenntnisgabeverfahren_anwendbar` | NO (SHA-bundle item 4, recorded) | gk_sprung defensively excluded from class predicate; others display-only | recorded — excluded from findings |

**Pre-noted CLASS-2 risks for a T-07 walk** (numbered for §8):

1. **Kenntnisgabe verdict hole (BW × T-07) — strongest pre-walk blocker.** BW cell (`stateOverrides.ts:4964-4973`) routes qualifying Anbauten via § 51 LBO BW Kenntnisgabe, but the `verfahren_indikation` pinned vocabulary (`personaBehaviour.ts:244-249`) has **no Kenntnisgabe form**, `classifyVerdictDirection` (`resolveProcedure.ts:292-310`) has no kenntnisgabe token (→ `null`), and no decision branch has a Kenntnisgabe kind. An obedient persona must mislabel § 51 (likely as "Genehmigungsfreistellung" — wrong institute) or improvise and fall to template-blind defaults. This is the same structural gap that bit T-05 Sachsen before the anzeige kind was added.
2. **`anbau_flaeche_m2` improvisation → wrong engine area.** Never directive-named; persona will plausibly emit `anbau_groesse_m2`/`anbauflaeche_m2`/prose-only m² → resolver miss → `detectAreaSqm` regex takes the FIRST `<digits> m²` in the fact corpus (plausibly Grundstücksgröße) → 180 m² backstop. Headline € is shielded by band mode, but the engine internals + any future engine promotion inherit the wrong area. Additionally the range guard `>= 20` (`costNormsMuenchen.ts:377`) rejects a legitimate 15 m² Wintergarten-Anbau.
3. **No volume channel for the template's decisive threshold.** The block *mandates* the persona compute and show BRI m³ (`t07-anbau.ts:235-250`); the figure can only land as an improvised key in the generic bucket. The 75 m³ tier survives solely via the `verfahren_indikation` string.
4. **Abstandsflächen compliance fact has no reader.** Cell + base prose call it the #1 hard constraint (`t07-anbau.ts:150-152`); an emitted `abstandsflaechen_eingehalten=false` drives no procedure conservatism, no risk row.
5. **GEG beheizt/unbeheizt discriminant uncapturable.** T-07's GEG question is beheizt vs unbeheizt (`t07-anbau.ts:92-94,153-154`); no fact channel — a heated Wintergarten without a facade-area fact never fires the GEG-Nachweis doc.
6. **Tragwerk-Anbindung mapping uncertainty.** `eingriff_tragende_teile` examples are removal-shaped (`personaBehaviour.ts:222`); an Anbau adds load — persona may improvise `tragwerk_anbindung`, silently dropping the structural-engineer forcing rule (`resolveRoles.ts:205`).
7. **`grenzbebauung` vs `grenzstaendig`** — cell prose uses both words; only the latter has readers.
8. Cosmetic inverse-CLASS-2 noise: `fassadenflaeche_m2` (T-03-shaped), `aenderung_aeussere_erscheinung`, `in_gestaltungssatzung` will inflate Key Data on T-07 — legitimate, just unscoped.

---

## §5 Corpus coverage per candidate state

Census: only `bw.json` has ingested bodies — **5 entries** (§§ 2, 5, 27f, 37, 56 — the BW×T-06 LRL-1 set, AKBW primary-source). All 15 other state files and `federal.json`: **0 bodies**; everything else is heading-only. **Zero § 27f-class absences found in T-07** — every § cited in any T-07 cell exists in its state's corpus file with a matching heading.

Per-state readiness grades (full §-tables in the audit evidence; operative highlights):

| State | Grade | Operative heading-only risk (F2-ancestor class) | Corpus stand |
|---|---|---|---|
| Bayern | B | **Art. 57 Abs. 1 Nr. 1 a (75 m³) + Abs. 7 — the single highest-stakes heading-only claims in T-07**; "Art.-6-Verstoß hebt Verfahrensfreiheit nicht auf" is body-level | primary-source, 2026-04-23 |
| BW | B+ | routing trio **§§ 50/51/52 heading-only on the 2025-03-18 heading layer** while `bw.ts:60-66` attests a 16.03.2026 Neufassung — mixed-version corpus; § 5/§ 37 bodies covered | mixed 2025/2026 |
| Hessen | C+ | **§ 64a "Erweiterte Genehmigungsfreistellung für die Errichtung von Wohngebäuden" — whether an ANBAU qualifies as "Errichtung" is precisely the body-level question**; § 52 Stellplätze €-direction | mirror, 2025-10-09 |
| NRW | C+ | §§ 63/64 routing + § 48 Stellplatz heading-only on a **2023-10-31 law stand** | primary headings, 2023-10-31 |
| Niedersachsen | B− | §§ 60/62/63 routing (§ 60 catalog lives in the Anhang — allowlisted, no body); §§ 46/47/48 € | mirror, 2025-06-25 |
| Berlin | B− | §§ 62/63 + § 49 (Berlin's no-Kfz-Stellplatzpflicht is body-level) | 2024-12-11 |
| Hamburg | B− | §§ 62/63 + § 49 Mobilität regime | 2025-11-18 (most current C-batch) |
| Bremen | B− | uniform pattern; corpus stand exactly matches the cell's claimed Neufassung | 2024-05-29 |
| Brandenburg | C | **§ 62 Bauanzeigeverfahren — BB-unique institute, conditions heading-only, oldest corpus**; cell correctly differentiates it from sibling §-62-Freistellung states (credit) | 2023-09-28 |
| SH | B− | uniform pattern; stand matches the cell's claim | 2024-12-13 |

**UNVERIFIED reform-era flags (never asserting non-existence — the § 27f lesson):** Bayern corpus contains Art. 82c "Bau-Turbo" + BauGB § 246e, both allowlisted (`bayernAllowedCitations.ts:60,:73`), neither wired into T-07 — whether the 2026 amendments restructured Art. 57's catalog is unverifiable from headings. BW: 2026 Neufassung effect on §§ 50/51/52 + ANHANG 1 unverified. NRW (2023 stand) and Brandenburg (2023 stand): post-2023 novelle activity unverifiable from the files.

> **Finding (new, walk-relevant): allowlist gaps in the four B2 deep states.** The BW, Hessen, NRW, and Niedersachsen T-07 cells all instruct `§ 23 BauNVO` and `§ 48 GEG`; **neither tuple is in those four allowlists** (B2-era short federal list: BauGB 30/34/35 + BauNVO 19 + GEG 10 only — e.g. `src/legal/states/bw.ts:407-409`). All C-batch states carry both. A persona obeying the cell gets flagged/downgraded by `citationLint` Layer-C on a live walk. Unguarded because `audit-instructed-citations.mts:17` explicitly skips federal instructions ("future slice").

> Also noted: `§ 15 LBO BW` (Brandschutz) and `§ 38 LBO BW` (Sonderbauten) — corpus headings match their usage in `bw.ts:382,:388` and `stateOverrides.ts:908` (the §0 ledger item was about *recording* the check, which remains unrecorded).

---

## §6 Gate coverage

Prebuild = 21 gates (`package.json:30`); CI adds `smoke:guards` (11 smokes, `package.json:74`) + walk/PDF smokes. Reach categories: (a) explicit T-07 fixture · (b) template-parameterized loop · (c) genuinely shared path · (d) never touches T-07.

**Executes T-07 input (would actually trip):**
- `smoke:t07-composer` — **(a)**, prebuild via `smoke:composer-parity` (`package.json:72`). **35 pins, runs green.** Fixtures: NRW (`:10`) + **Bayern (`:20`) — meta-sweep provenance CONFIRMED** (added in `2b8ae5d`, in-file marker `:17-18` "Meta-sweep item 3c — Bayern fixture"). Pin quality: **1 of 35 is Anbau-specific** (`:15` — `/Anbau/` in reasoning_de, no cross-intent bleed) + 4 baseline-citation pins carry anbau-specific values (NRW § 64 vereinfacht, Bayern Art. 59, flips to § 62 / Art. 57); the other ~30 are the shared composerParity harness (`composerParity.mts:127-168`: baseline/flip/4-surface-equality/EN-DE anchor). **Not asserted:** the 75 m³ threshold (the flip injects the verdict string wholesale — threshold logic never exercised), Abstandsflächen consequences, anbau-area cost basis, Bestand roles, documents.
- `audit:four-class:gate` — **(b) executed**, prebuild. All 128 cells incl. T-07×16 through real resolvers (`audit-four-class-sweep.mts:63-67,124-171`); cost check **vacuous for T-07** (anbau not in the Bestand set, `:148`).
- `smoke:thin-state` — **(b) at intent level**, CI. 142 pins; the fix-proof loop drives `intent:'anbau'` across 6 states (`smoke-thin-state-propagation.mts:144-147`).

**Static T-07 content reach (b):** `verify:template-tail-noop` (15 `'T-07:*'` acknowledged keys, `:365-379`), `verify:template-tail-citations` Tier-1/2 (loop `:484`) — **Tier-3 `OPERATIVE_BODY_REQUIRED` = `{'T-06|bw': […]}` only, zero T-07 entries (`:575-583`)** — the exact BW×T-06 walk-1 failure class is unguarded for T-07; `verify:forbidden-paragraphs`; `audit:instructed` (`:104-111`); `verify:citations:strict`; smokeWalk static required-blocks (`smokeWalk.mjs:1218-1233`); pdf-matrix band-leak loop (`smoke-pdf-matrix.mts:391-397` — strings only).

**Vacuous on T-07 (stay green through a T-07-specific regression):** `smoke:cost-procedure` (T-01 engine path only — T-07's band path never executed; own header concedes it), `smoke:export-parity` (single MV×T-03 fixture), `smoke:class2-capture` (T-03), `smoke:role-gating` (no anbau fixture), `smoke:pdf-text`/`pdf-matrix` render (no T-07 PDF ever rendered in CI), `verify:bayern-sha` (scope note excludes template tails), plus all locale/env/bundle/z-index gates.

**Live-LLM T-07 coverage:** operator-gated only — smokeWalk `--live` carries an explicit **T-07 Wintergarten seed** (`smokeWalk.mjs:167-187`, `SMOKE_T07_PROJECT_ID`, expects Art. 57 Abs. 1 Nr. 1 a + 48 m³ math; exits 2 without secrets) and `smoke:matrix` cell #11 = bayern×T-07 with the weak anchor `/Anbau|Abstand/i` (`smoke-walk-matrix.mjs:157`); **no non-Bayern T-07 cell in the matrix**.

**Wiring note:** `smoke:t07-composer` has no dedicated CI step; it reaches CI only transitively via the Build step's npm `prebuild` hook (`test.yml:43-44` → `package.json:30`) — it would silently leave CI if the build script is ever refactored off the hook.

---

## §7 Regression-immunity — every closed class, judged on T-07 reach

Verdicts: **COVERED** (guard reaches T-07 via (a) fixture / (b) parameterized loop / (c) proven-shared code path) · **PARTIAL** · **UNCOVERED-ON-T07**. Both smokes re-run green during the audit (`smoke-thin-state` 142/0, `smoke-t07-composer` 35/0, `smoke-t06-composer` 85/0, `smoke-export-parity` 17/0, `smoke-plz-bundesland` 48/0).

| # | Class | Guard (file:line) | T-07 reach | Verdict |
|---|---|---|---|---|
| 1 | RED-1 procedure self-contradiction | fix `composeLegalDomains.ts:81-87,206-235` (decision is required param; old regex deleted `:28-34`); guard `composerParity.mts:95-168` instantiated by `smoke-t07-composer.mts:13-22`, prebuild | (a)+(c) | **COVERED** |
| 2 | Cost-consistency, area axis | `COST_BASIS_FIELD_BY_TEMPLATE['T-07']=['anbau_flaeche_m2']` (`costNormsMuenchen.ts:341`); chain `:426-437→:361-402→:299-305→180` (`:355`; no 400 m² fallback exists anywhere) | headline shielded by band route (`:624-629,:680-689`; consumers `AtAGlance.tsx:127`, `CostTimelineTab.tsx:117`, `composeExecutiveRead.ts:140`, `exportPdf.ts:756`) but **`anbau_flaeche_m2` is a dead pin — appears nowhere outside costNormsMuenchen.ts, no fixture, no directive** | **PARTIAL** |
| 3 | Cost-consistency, multiplier axis (GK→cost detectKlasse) | fix `costNormsMuenchen.ts:474-480,490-503` (typed-fact-first); guard `smoke-cost-procedure.mts:32-74` (CI) | (c) — shared resolver, 4 consumers unconditional; multiplier structurally cannot move T-07's band headline | **COVERED** |
| 4 | Fact→surface propagation | guard `smoke-thin-state-propagation.mts:142-148` (anbau intent × 6 states) + `audit-four-class-sweep.mts:124-173` (×T-07 cells, prebuild) + t07 parity flips | (b) | **COVERED** |
| 5 | Fact→capture thin-state gap | directives `personaBehaviour.ts:195-229,254-274`; guards `audit-four-class-sweep.mts:222-252` (READER_EXACT_KEYS — 5 of 9 keys list `anbau` in relevantIntents) + `smoke-class2-capture.mts` (MV fixture) | (b)+(c); **live emission never confirmed on a T-07 walk** (sweep marks CLASS-2 live-capture INFO by design, `:29-32,243-251`) | **PARTIAL** |
| 6 | CLASS-1 two-sources-of-truth | `composeLegalDomains.ts:234-267` now citation-sniffing only; repo-wide grep: every verdict-classifying regex lives inside `resolveProcedure.ts` itself (`:295-306,659,684,711` — the canonical source); consumers switch on `decision.kind` (`resolveProcedures.ts:33`, `composeTimeline.ts:200`, `deriveBaselineRoles.ts:286`, `exportPdf.ts:903-904`). Residual: `pdfSections/executive.ts:105` `inferPriority` — cosmetic pill bucketer, never renders a verdict | (a)+(c) | **COVERED** |
| 7 | CLASS-2 capture contract | guard `smoke-class2-capture.mts:30-93` (T-03/MV fixture; pipeline `applyExtractedFacts` proven template-blind `projectStateHelpers.ts:383-410`) + READER_EXACT_KEYS prebuild audit | (b)+(c) for generic keys; **T-07's own `anbau_flaeche_m2` outside the contract entirely** (not in READER_EXACT_KEYS, not directive-named, not pinned) | **PARTIAL** |
| 8 | CLASS-3 keyword fragility (incl. legal-landscape substring) | fix: lookahead `genehmigungsfrei(?!gestellt\|stellung)` at `resolveProcedure.ts:298,:659` + dedicated Freistellung branch `:684-704` + keyword-free §-comparison classifier `:973-1010`; guards `smoke-t01-composer.mts:16` (fix-proof, T-01) + `composerParity.mts:117-122` (structural, **fires only on Freistellung-yielding fixtures — vacuous on T-07's current fixtures**) + anbau §-classifier pins (`smoke-thin-state:142-148`) | (c) shared source; residuals reachable on T-07: **anzeige branch `:711-729` = §3's unguarded demolition prose**; `extractProcedureCitation` no-space edge (accepted YELLOW, `audit-four-class-sweep.mts:57-60`) | **PARTIAL** |
| 9 | CLASS-4 out-of-corpus + heading gate + qualifier gate | runtime: `citationHeadingGate.ts` (state-keyed concepts, no template branch) + `citationLint.ts:409-614` Layer-C; scripts: `smoke-citation-heading-gate.mts` (6 scenarios), `smoke-qualifier-gate.mts`; build: `verify:citations:strict` + `audit:instructed:104-112` + `audit-four-class-sweep.mts:178-212` all loop T-07 cells; smokeWalk T-07 Wintergarten seed (`:167-187`) | (b)+(c)+(a-live, env-gated) | **COVERED** |
| 10 | .md parity (resolveDocuments/resolveRoles) | fix `exportMarkdown.ts:158,:213` (unconditional resolver calls, no template branch in file); guard `smoke-export-parity.mts` 17 pins (MV×T-03) | (c) | **COVERED** (fixture diversity T-03-only) |
| 11 | Stadtarchiv city fallback | fix `archiveLabel.ts:15-35`; guards `smoke-export-parity.mts:54-67` + `smoke-pdf-text.mts:319-334`; consumer `exportPdf.ts:604` gated only on Area A | (c) | **COVERED** |
| 12 | Derived-GK Key-Data carve-out | single source `deriveGebaeudeklasse.ts:123-129` — **T-07 NOT in the carve-out** (T-04 'use-conversion', T-06 'storey-addition' only); consumers `exportPdf.ts:1216` (inside `!hasExplicitKlasse` `:1195-1230`), `AtAGlance.tsx:87` | (c) machinery shared, but the exclusion is an unreviewed design decision: an Anbau that pushes a Nutzungseinheit across the 400 m² threshold (GK 1/2→3, 4→5) gets a freshly derived GK with **no pre/post-extension model** — the same no-before/after rationale that carved out T-06 | **PARTIAL** |
| 13 | freistehend default | `TEMPLATE_FREISTEHEND_DEFAULT['T-07'] = false` — explicit, conservative (attached ⇒ higher GK; fact override `:185-197`; absent fact ⇒ ASSUMED `:221`) | full table quoted `deriveGebaeudeklasse.ts:82-100` | **COVERED** |
| 14 | gk_sprung row suppression | predicate `isExplicitKlasseFactKey` `deriveGebaeudeklasse.ts:157-161`; consumers `exportPdf.ts:1195`, `AtAGlance.tsx:213` (unconditional); pins `smoke-t06-composer.mts:125-126` | (c) | **COVERED** |
| 15 | Surveyor rationale template-awareness | `deriveBaselineRoles.ts:377-395` — `case 'anbau': return NEW_BUILD_ROLES(citations,'bestand')`; **explicit anbau pin** `smoke-t06-composer.mts:114-115` | **(a)** | **COVERED** |
| 16 | Recommendation semantic dedup (shared selector, 4 surfaces) | `resolveRecommendations.ts:77-102` (zero templateId reads); surfaces `composeDoNext.ts:53`, `exportPdf.ts:416,1078`, `exportMarkdown.ts:84`; source-grep wiring pins `smoke-t06-composer.mts:181-182` | (c) — wiring pins guard all templates at once | **COVERED** |
| 17 | abbruch_typ / applicableTemplates quarantine | registry `systemFlagFilter.ts:52-56` (one entry); consumers parameterized by `state.templateId` (4 surfaces); pins `smoke-t06-composer.mts:184-199` | (c) — `'T-07' ∉ ['T-05']` fires by identical lookup; **registry has exactly one key — open-by-design for future scoped keys** | **COVERED** for abbruch_typ |
| 18 | Internal/system-flag suppression | `systemFlagFilter.ts:25-34` (pure key predicate); pins `smoke-t06-composer.mts:203-209` | (c) template-blind by construction | **COVERED** |
| 19 | PLZ→Bundesland GeoNames table | `plzBundesland.generated.ts` (946 entries) + `smoke-plz-bundesland.mts` 48 pins (CI, first in smoke:guards) | (c) — runs before template selection exists | **COVERED** |
| 20 | MAX_TOKENS budget | `anthropic.ts:69` (`MAX_TOKENS = 2560`, ledger `:45-57`), gauge `:330` + `streaming.ts:245` | (c) module constant on every request | **COVERED** |
| 21 | Edge deploy-verify rail | `verify:edge-fingerprint` in prebuild; graph root `edgeFingerprint.mjs:20` — **walker executed read-only: the 57-file graph includes `stateOverrides.ts` AND `t07-anbau.ts`** — a T-07 cell edit moves the fingerprint | (c), T-07-inclusive proven | **COVERED** |
| 22 | Stale-Vercel rail | `verify-deploy.mjs:52-80` (version.json SHA) + `verify-bundle-size.mjs:33` (320 KB ledger `:17-24`) in build | deployment-level | **COVERED** |

**Numbered guard-gap findings from this section** (closed classes whose guards do not reach T-07 specifically):
- **G-1.** Tier-3 operative-body gate has zero T-07 entries (`verify-template-tail-citations.mts:575-583`) — the F2/walk-1 class (heading-only § whose body reverses the cell's claim) is unguarded for every T-07 cell, while §5 shows the routing §§ are heading-only in all 16 states.
- **G-2.** The anzeige-on-anbau wording leak (§3) has no pin anywhere; `composerParity`'s Freistellung structural pin is vacuous on T-07's current fixtures (none yields Freistellung/anzeige).
- **G-3.** `anbau_flaeche_m2` is outside every guard (classes 2 and 7) — a dead pin protecting nothing until directive-named.
- **G-4.** No T-07 PDF is ever rendered in CI (`smoke-pdf-text`/`pdf-matrix` fixture sets), and no T-07 fixture exists in export-parity/class2-capture/role-gating — all shared-path-covered, but the Jena lesson says fixture diversity is what catches the next per-template branch someone adds.
- **G-5.** `smoke:matrix` has no non-Bayern T-07 cell, and the Bayern cell's anchor `/Anbau|Abstand/i` is two words over a 5-turn transcript.

---

## §8 Walk-readiness verdict

### Verdict: **GO-WITH-KNOWN-RISKS**

The spine is walk-ready: explicit wizard→T-07 dispatch with no keyword fragility at intake; all 15 state cells REAL with corpus-verified, heading-matched §§ (zero out-of-corpus citations); the procedure/cost/roles/recommendation surfaces are decision-first and parity-gated with an executed T-07 prebuild smoke (35 pins incl. Bayern); the ancestral exportPdf fallthrough is proven closed; both deploy rails stand and the edge fingerprint provably covers T-07 content.

**Known-risk list to carry into the walks** (report-only; nothing fixed this turn):

| # | Risk | Severity | Ref |
|---|---|---|---|
| R1 | Anzeige verdict renders demolition wording + demolition-§ fallback on 5 surfaces — *will fire live* in any state whose cell offers an Anzeige route (Bayern Art. 57 Abs. 7, BB § 62) | HIGH (trust-contract) | §3 |
| R2 | BW Kenntnisgabe verdict-vocabulary hole (no 6-form entry, no classifier token, no kind) | HIGH (BW-only) | §4 risk 1 |
| R3 | `anbau_flaeche_m2` never directive-named; ≥20 m² guard rejects small Anbauten; engine area silently wrong | MEDIUM (headline shielded by band) | §4 risk 2, §7 G-3 |
| R4 | 75 m³ BRI has no fact channel; threshold lives only in prompt prose + the verdict string | MEDIUM | §4 risk 3, §1 #6 |
| R5 | Documents fallthrough: no Anbau branch; structural docs gated on `eingriff_tragende_teile`, which has removal-shaped directive examples | MEDIUM | §1 #1, §4 risk 6 |
| R6 | GK carve-out excludes T-07 — derived GK on an existing-building modification, no pre/post model (NE-threshold edge) | MEDIUM | §7 #12 |
| R7 | All operative routing §§ heading-only in every state (only BW §§ 5/37 have bodies); HE § 64a "Errichtung" scope and BB § 62 conditions are body-level questions; Tier-3 gate has no T-07 entries | MEDIUM (F2-ancestor) | §5, §7 G-1 |
| R8 | BW/HE/NRW/NI allowlists missing BauNVO § 23 + GEG § 48 that their own T-07 cells instruct → live lint downgrade | MEDIUM (will fire on the deep walk) | §5 |
| R9 | GEG beheizt/unbeheizt discriminant uncapturable; geg_trigger is a Sanierung heuristic | LOW-MEDIUM | §4 risk 5 |
| R10 | Full Bauantrag Gantt + "Baugenehmigung issued" milestone on verfahrensfrei/anzeige outcomes; generic permit docs on a notification-only decision | LOW | §1 #4, §3 |
| R11 | Five known-open items recorded only in session memory (§0) | PROCESS | §0 |

### Recommended walk pair

**Deep state: Hessen (HBO numbering family — never walked).** The only deep-content state not yet walked (walked: BY, RLP, NRW, ST, BW, MV, SN, SL, NI, TH). 40-line cell (`stateOverrides.ts:4996-5035`) with the genuinely distinct **§ 64a Erweiterte Genehmigungsfreistellung** institute — its heading-only "Errichtung von Wohngebäuden" scope question (R7) is exactly what a live walk adjudicates; corpus current to 2025-10-09 with full presence/heading match; distinct ladder (§§ 64/64a/65 — neither the standard § 61-64 ladder nor BW/RLP). The cell offers **no verfahrensfrei route**, so a modest Anbau exercises the Freistellung-vs-vereinfacht boundary (B-Plan-dependent) — and R8 (missing § 23 BauNVO / § 48 GEG allowlist tuples) will fire observably.
**Address profile:** Wiesbaden — Taunusstraße 24, **65183 Wiesbaden** (PLZ verified in `plzBundesland.generated.ts` via longest-prefix `6518 → hessen`; executed lookup this audit). Alternate: Kassel — Goethestraße 12, **34117 Kassel** (`341 → hessen`, and `['34117 Kassel','hessen']` is pinned in `smoke-plz-bundesland.mts:52`). Profile: single-storey ~35 m² side extension to a 1960s EFH inside a qualified B-Plan area (probes § 64/§ 64a routing, Abstandsflächen at the side boundary, GEG-beheizt question R9).

**Thin state: Brandenburg (BbgBO — never walked).** Distinct procedure tier by design: **§ 62 Bauanzeigeverfahren is the BB-unique anzeige institute** (`stateOverrides.ts:5523-5570`), giving the pair the requested tier spread — Hessen full/Freistellung Verfahren vs Brandenburg anzeige-eligible — and **deliberately exercises R1 live** (the anzeige-kind path on a non-demolition template; the walk should expect and document the "(Abbruchanzeige)" leak rather than be surprised by it). Distinct numbering (post-`a5f7ebb` renumber fix § 66/§ 68), the C-batch cell that correctly differentiated the BB institute (§5 credit). Known handicaps, accepted: oldest corpus stand (2023-09-28) → treat any reform-era § the persona raises as UNVERIFIED, never absent; thin banner stays ON.
**Address profile:** Potsdam — Hegelallee 8, **14467 Potsdam** (PLZ verified: `1446 → brandenburg`; `['14467 Potsdam','brandenburg']` is pinned in `smoke-plz-bundesland.mts:22` — the exact PLZ the old table mis-detected as Berlin, now guarded). Profile: ~20–25 m² rear/side Anbau to a Gründerzeit row house north of the centre (attached → freistehend=false default exercised; Grenzbebauung question; Bauanzeige eligibility per § 62 BbgBO).

**Alternate thin** (if the operator prefers not to walk into R1 knowingly): Hamburg — Hofweg 45, **22085 Hamburg** (`2208 → hamburg`, verified; `['20095 Hamburg','hamburg']` pinned at `smoke-plz-bundesland.mts:24`) — most current C-batch corpus (2025-11-18) and the distinct § 49 "Grundstücksbezogene Mobilität" Stellplatz regime, but it loses the anzeige-tier spread.

---

**STOP.** No fixes, no branches, no directive edits were made. Bayern SHA `41b37961` verified unchanged at audit start; no file outside `docs/` was written.
