import { PagePlaceholder } from './_PagePlaceholder'

export function TurnDeepDive() {
  return (
    <PagePlaceholder
      title="Turn deep-dive"
      description="The replay artifact. Trace overview · span Gantt · persona snapshot (system prompt blocks, messages, tool input/output) · linked project_events."
      arrivingIn="commit 9"
    />
  )
}
