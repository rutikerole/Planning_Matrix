import { PagePlaceholder } from './_PagePlaceholder'

export function ProjectInspectorList() {
  return (
    <PagePlaceholder
      title="Projects"
      description="Sortable, filterable inventory of every project with its cumulative trace metrics — turns, tokens, cost, error rate."
      arrivingIn="commit 8"
    />
  )
}
