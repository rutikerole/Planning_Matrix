import { PagePlaceholder } from './_PagePlaceholder'

export function ProjectInspectorDetail() {
  return (
    <PagePlaceholder
      title="Project detail"
      description="Project context (header, cumulative metrics, state summary) on the left; turn-by-turn timeline on the right. Click a turn to drop into the deep-dive."
      arrivingIn="commit 8"
    />
  )
}
